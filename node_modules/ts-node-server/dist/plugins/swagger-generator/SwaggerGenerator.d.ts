import { FastifyInstance } from 'fastify';
import { Container } from 'inversify';
import { WireupEndpoint } from '../common/CommonUtil';
import { ParamOptions } from '../common/param/ParamOptions';
import { OpenApiConf } from './models/OpenApiConf';
import { OpenApiMethod } from './models/OpenApiMethod';
export declare class SwaggerGenerator {
    static INDEX_HTML_TEMPLATE: string;
    static translateUrl(url: string): string;
    static buildConfiguration(container: Container, userDefinedConfiguration?: OpenApiConf): OpenApiConf;
    static buildAuthenticationConfiguration(container: Container, configuration?: OpenApiConf): OpenApiConf;
    static buildConfigurationForParameter(configuration: OpenApiConf, methodOptions: OpenApiMethod, paramOptions: ParamOptions): OpenApiConf;
    static buildConfigurationForEndpoint(endpoint: WireupEndpoint): OpenApiConf;
    static getPlugin(instance: FastifyInstance, opts: {
        container: Container;
        configuration: OpenApiConf;
    }, next: (err?: Error) => void): void;
    private static logger;
}
