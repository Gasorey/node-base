import { Token } from 'auth-header';
import { Container } from 'inversify';
import { AuthOptions } from '../plugins/common/method/AuthOptions';
import { Request } from '../types';
import { AuthProvider } from './AuthProvider';
export declare class AuthUtil {
    static parseAuthorizationHeader(request: Request): Token;
    static normalizeAuthOptions(authOptions: string | string[] | {
        [providerName: string]: AuthOptions;
    }): AuthOptions[];
    static getAuthProviders(container: Container, authOptions: AuthOptions[]): Array<{
        provider: AuthProvider;
        options: AuthOptions;
    }>;
}
