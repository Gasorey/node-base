import { AuthOptions } from '../plugins/common/api';
export declare class AdminController {
    private options;
    static decorateMethods(options: AdminOptions): void;
    private logger;
    constructor(options: AdminOptions);
    setLoggingLevel(level: string): Promise<void>;
    enableInspector(port: number): Promise<{
        url: string;
    }>;
    disableInspector(): Promise<void>;
}
export interface AdminOptions {
    auth: string | string[] | {
        [provider: string]: AuthOptions;
    };
    inspectorPort?: number;
    inspectorHost?: string;
}
