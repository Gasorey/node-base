import { Healthcheck } from '../healthcheck/api';
import { MongoService } from './MongoService';
export declare class MongoHealthcheck implements Healthcheck {
    private mongoService;
    constructor(mongoService: MongoService);
    getName(): string;
    check(): Promise<any>;
}
